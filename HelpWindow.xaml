<Window x:Class="wLabelDesigner.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="wLabelDesigner Help"
        Width="900"
        Height="720"
        MinWidth="700"
        MinHeight="520"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="HelpHeading" TargetType="TextBlock">
            <Setter Property="FontSize" Value="19" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,26,0,10" />
        </Style>
        <Style x:Key="HelpSubheading" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,15,0,6" />
        </Style>
        <Style x:Key="HelpBody" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="LineHeight" Value="20" />
        </Style>
        <Style x:Key="ShortcutKey" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            <Setter Property="Width" Value="145" />
        </Style>
        <Style x:Key="ShortcutDescription" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
    </Window.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="*" />
            <RowDefinition Height="64" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="28,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Background="#FAFBFC">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="wLabelDesigner User Guide" FontSize="23" FontWeight="SemiBold" />
                <TextBlock Text="Design and print thermal labels on Windows" Foreground="{StaticResource MutedTextBrush}" Margin="0,3,0,0" />
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="32,8,42,32" MaxWidth="780" HorizontalAlignment="Left">
                <TextBlock Text="Getting started" Style="{StaticResource HelpHeading}" />
                <TextBlock Style="{StaticResource HelpBody}"
                           Text="Create a label, set its physical width, height, and printer DPI in the top bar, then drag tools from the left rail onto the white label. Move elements directly, resize them with their handles, and use the contextual top bar for text or shape formatting." />

                <TextBlock Text="Selecting and editing" Style="{StaticResource HelpHeading}" />
                <TextBlock Style="{StaticResource HelpBody}"
                           Text="Click an element to select it. Shift-click toggles additional elements in the selection. Click empty workspace or press Escape to clear the selection. Drag a selected element to move the whole selection. Use the eight handles to resize regular elements; lines use two endpoint handles." />
                <TextBlock Style="{StaticResource HelpBody}" Margin="0,8,0,0"
                           Text="Double-click text to edit it directly. Enter creates a new line, Ctrl+Enter commits, Escape cancels, and clicking away commits. Barcode and QR data is edited in the contextual bar above the workspace." />
                <TextBlock Style="{StaticResource HelpBody}" Margin="0,8,0,0"
                           Text="For barcodes, choose the format before entering content. Invalid length, unsupported characters, and incorrect EAN, UPC, or ITF check digits are shown below the content field and must be fixed before printing or exporting." />
                <TextBlock Style="{StaticResource HelpBody}" Margin="0,8,0,0"
                           Text="Use Show text to include or hide the human-readable barcode value beneath the bars. This setting is saved separately for each barcode." />
                <TextBlock Style="{StaticResource HelpBody}" Margin="0,8,0,0"
                           Text="Drag the circular handle above a selected element to rotate it. Hold Shift while dragging to snap the angle to 15-degree increments, or enter an exact rotation in the element panel on the right." />
                <TextBlock Style="{StaticResource HelpBody}" Margin="0,8,0,0"
                           Text="When two or more elements are selected, the contextual toolbar provides edge and centre alignment commands. With three or more selected elements, Space H and Space V distribute them evenly." />

                <TextBlock Text="Styling elements" Style="{StaticResource HelpHeading}" />
                <TextBlock Style="{StaticResource HelpBody}"
                           Text="Use the contextual formatting bar to choose text and shape colors, opacity, corner radius, stroke pattern, vertical alignment, line spacing, and letter spacing. Auto fit reduces text size when necessary; a red exclamation mark warns when text still overflows its bounds." />

                <TextBlock Text="Layers, locking, and visibility" Style="{StaticResource HelpHeading}" />
                <TextBlock Style="{StaticResource HelpBody}"
                           Text="The Layers panel lists elements from front to back. Select a layer, then use Front, Up, Down, or Back to change its stacking order. The same commands are available from the Arrange menu." />
                <TextBlock Style="{StaticResource HelpBody}" Margin="0,8,0,0"
                           Text="Lock an element to prevent its position from changing. A position-locked element can still be selected, resized, rotated, formatted, deleted, and reordered in the Layers panel. Hidden elements remain available in the Layers panel but do not appear on the label, in print preview, or in printed output." />

                <TextBlock Text="Canvas navigation and alignment guides" Style="{StaticResource HelpHeading}" />
                <TextBlock Style="{StaticResource HelpBody}"
                           Text="Use the controls in the bottom bar to zoom, show or hide the millimetre grid and rulers, and choose the visual grid size. The grid and rulers are design aids and are not printed. Use middle-button drag, or hold Space and drag with the left button, to reposition the label in the white workspace." />
                <TextBlock Style="{StaticResource HelpBody}" Margin="0,8,0,0"
                           Text="Red guides appear temporarily when moved elements approach the edges or centres of the label and other elements. Elements always move freely: the grid and alignment guides never change or snap their position." />

                <TextBlock Text="Keyboard shortcuts" Style="{StaticResource HelpHeading}" />

                <TextBlock Text="Files and application" Style="{StaticResource HelpSubheading}" />
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ctrl+N" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Create a new label" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ctrl+O" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Open a .fckbartndr template" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ctrl+S" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Save the current template" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ctrl+P" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Open print preview and configure printer/copies" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="F1" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Open this help window" Style="{StaticResource ShortcutDescription}"/></Grid>

                <TextBlock Text="Editing" Style="{StaticResource HelpSubheading}" />
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ctrl+Z" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Undo" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ctrl+Y" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Redo" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ctrl+Shift+Z" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Redo (alternative shortcut)" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ctrl+C / X / V" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Copy, cut, or paste an element" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ctrl+D" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Duplicate the selected element" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Delete" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Delete all selected elements" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Escape" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Clear selection; cancels active inline text editing" Style="{StaticResource ShortcutDescription}"/></Grid>

                <TextBlock Text="Navigation and positioning" Style="{StaticResource HelpSubheading}" />
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Tab / Shift+Tab" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Select the next or previous element" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Arrow keys" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Move the selection by 0.5 mm" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Shift+arrows" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Move the selection by 5 mm" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Shift+click" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Add or remove an element from the selection" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ctrl+wheel" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Zoom around the mouse pointer" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ctrl++ / Ctrl+-" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Zoom in or out" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ctrl+0" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Reset zoom to 100%" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Middle-drag" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Pan the canvas" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Space+drag" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Pan the canvas with the left mouse button" Style="{StaticResource ShortcutDescription}"/></Grid>

                <TextBlock Text="Text editing" Style="{StaticResource HelpSubheading}" />
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Double-click" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Edit a text element in place" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Enter" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Insert a new line while editing text" Style="{StaticResource ShortcutDescription}"/></Grid>
                <Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition Width="145"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Text="Ctrl+Enter" Style="{StaticResource ShortcutKey}"/><TextBlock Grid.Column="1" Text="Commit inline text editing" Style="{StaticResource ShortcutDescription}"/></Grid>

                <TextBlock Text="Templates and printing" Style="{StaticResource HelpHeading}" />
                <TextBlock Style="{StaticResource HelpBody}"
                           Text="Templates use the .fckbartndr extension and JSON data. They store label size, DPI, all elements and styles, printer choice, and default copy count. Print opens a preview that uses the same renderer as physical output. Choose a printer and copies, then press Print. Save the document afterward when the title shows an asterisk." />

                <TextBlock Text="About" Style="{StaticResource HelpHeading}" />
                <TextBlock Style="{StaticResource HelpBody}"
                           Text="wLabelDesigner is a lightweight .NET 10 WPF application for designing and printing thermal labels, optimized for common 203 and 300 DPI Windows label printers." />
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" Padding="20,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Background="#FAFBFC">
            <Button Content="Close" IsCancel="True" MinWidth="90" HorizontalAlignment="Right" VerticalAlignment="Center" />
        </Border>
    </Grid>
</Window>
